package org.kie.pmml.compilation.commons;

import java.util.Arrays;
import java.util.Collections;

import org.kie.pmml.api.models.TargetValue;

public class TargetValueTemplate {

    private TargetValue getTargetValue() {
        TargetValue targetValue = new TargetValue(value, displayValue, priorProbability, defaultValue);
    }

}
