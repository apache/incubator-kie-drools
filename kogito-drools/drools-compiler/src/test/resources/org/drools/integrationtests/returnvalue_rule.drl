package org.drools.test;

import org.drools.Person;

rule "simple rule"
    when
        Person( $age1 : age )
        Person( age == ( $age1 + 2 ) )
    then
		System.out.println( "hello" );
end    