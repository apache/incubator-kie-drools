{
    if (Objects.equals(%s, input))
        return %s;
    else if (Objects.equals(%s, input))
        return %s;
    else if (Objects.equals(%s, input))
        return %s;
    else
        return 0.0;
}