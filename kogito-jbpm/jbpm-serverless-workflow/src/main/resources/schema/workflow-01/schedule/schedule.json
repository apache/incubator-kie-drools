{
  "type": "object",
  "javaType": "org.jbpm.serverless.workflow.api.schedule.Schedule",
  "description": "Start state schedule definition",
  "properties": {
    "interval": {
      "type": "string",
      "description":  "Time interval describing when the workflow starting state is active"
    }
  },
  "required": [
    "interval"
  ]
}