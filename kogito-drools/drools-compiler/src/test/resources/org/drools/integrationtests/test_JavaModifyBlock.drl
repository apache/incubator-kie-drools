package org.drools;

global java.util.List results;

rule "test modify block"
when
    $p: Person( status == "hungry" )
    $c: Cheese( )
then
    retract( $c );
    modify( $p ) { 
        setStatus( "full" ), 
        setAge( $p.getAge() + 1 ) 
    }
    results.add( $p );
end