<?xml version="1.0" encoding="UTF-8"?>
<section version="5.0"
         xsi:schemaLocation="http://docbook.org/ns/docbook http://www.docbook.org/xml/5.0/xsd/docbook.xsd http://www.w3.org/1999/xlink http://www.docbook.org/xml/5.0/xsd/xlink.xsd"
         xml:base=".."
         xml:id="sect-dashbuilder-dataproviders-sql_provider"
         xmlns="http://docbook.org/ns/docbook"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns:xs="http://www.w3.org/2001/XMLSchema"
         xmlns:xlink="http://www.w3.org/1999/xlink"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:ns="http://docbook.org/ns/docbook">

  <title>Reading data from an SQL query</title>

  <para>You can create a data provider to query a relational database. Go to
    <emphasis>Administration &gt; Data providers</emphasis>and click on
    '<emphasis>Create new data provider</emphasis>'. Choose the '<emphasis>SQL
      Query</emphasis>' option and fill the form with the data provider name and
    the SQL query that will retrieve the data.
  </para>

  <figure>
    <title>New SQL data provider form</title>
    <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata align="center" fileref="images/DataProviders/SQLProviderCreationForm.png"/>
        </imageobject>
      </mediaobject>
    </screenshot>
  </figure>

  <para>In this form you have the ability to select the data source where the
    data comes from. By default the local data source is selected but you can
    define new connections to external data sources. To do this you should go to
    the '<emphasis>Administration &gt; External connections</emphasis>' section
    and from there you can create a new data source connection.
  </para>

  <figure>
    <title>Data sources management panel</title>
    <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata fileref="images/DataProviders/DataSourcesTable.png"/>
        </imageobject>
      </mediaobject>
    </screenshot>
  </figure>

  <figure>
    <title>New data source creation form</title>
    <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata fileref="images/DataProviders/DataSourceCreationForm.png"/>
        </imageobject>
      </mediaobject>
    </screenshot>
  </figure>

  <para>Let's get back to the creation of our SQL data provider. Once the data
    source has been selected and the query is typed in, you can click on the
    'Try' button, and if the query is successful you will get the following
    message.
  </para>

  <figure>
    <title>SQL query input filed</title>
    <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata align="center" fileref="images/DataProviders/SQLProviderQueryField.png"/>
        </imageobject>
      </mediaobject>
    </screenshot>
  </figure>

  <para>After that, you can rename the name of the properties to provide a
    more user friendly name.
  </para>

  <figure>
    <title>SQL provider columns configuration panel</title>
    <screenshot>
      <mediaobject>
        <imageobject>
          <imagedata align="center" fileref="images/DataProviders/SQLProviderColumnsTable.png"/>
        </imageobject>
      </mediaobject>
    </screenshot>
  </figure>

  <para>Finally, just click the '<emphasis>Save</emphasis>' button to confirm
    the creation of the data provider:
  </para>

</section>
