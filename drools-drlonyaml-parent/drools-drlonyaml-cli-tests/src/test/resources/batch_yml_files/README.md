Using this directory to test CLI command producing in batches a conversion.
Only expecting to be git-commited files with .yml extension in here;
files with .drl extension are ignored in the .gitignore file.
