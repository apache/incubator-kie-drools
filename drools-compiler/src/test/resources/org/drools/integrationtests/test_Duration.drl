package org.kie.test;

import org.kie.Cheese;
import org.kie.Person;

global java.util.List list;

rule delayed
    duration 100
    when
        cheese : Cheese( )
    then
        list.add( cheese );
end   
