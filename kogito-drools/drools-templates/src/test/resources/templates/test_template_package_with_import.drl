template header
name

package This_is_a_ruleset;
import org.drools.template.jdbc.Person;

template "template1"
name
rule "Rule_@{row.rowNumber}"
    when
        User( name == "@{name}" )
    then
end
end template

