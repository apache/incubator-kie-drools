package org.drools.test;

import org.drools.Cheese;

global java.lang.Integer total;

rule "like stilton"
    when
        $cheese : Cheese( $type:type == "stilton" )
        eval( $cheese.getPrice() == 5 )
    then
        System.out.println("I like " + $type + " : " +  total);
end  