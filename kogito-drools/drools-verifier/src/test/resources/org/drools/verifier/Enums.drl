package org.drools.benchmark.manners


import org.test.Person;
import org.test.Context;
import org.test.Guest;
import org.test.Count;

rule assignFirstSeat
    when
        context : Context( state == Context.START_UP )
        guest : Guest()
        count : Count()
    then
		# 
end