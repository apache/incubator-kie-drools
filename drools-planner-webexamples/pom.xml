<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.kie.planner</groupId>
    <artifactId>drools-planner</artifactId>
    <version>6.0.0-SNAPSHOT</version>
  </parent>

  <artifactId>drools-planner-webexamples</artifactId>
  <packaging>war</packaging>

  <name>Drools Planner webexamples</name>
  <description>
    Drools Planner optimizes automated planning by combining metaheuristic search algorithms with rule
    engine powered score calculation. This is the drools-planner-webexamples module which contains web examples
    on how to use Drools Planner in a war file.
  </description>

  <dependencies>
    <!-- Internal dependencies -->
    <dependency>
      <groupId>org.kie.planner</groupId>
      <artifactId>drools-planner-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.kie.planner</groupId>
      <artifactId>drools-planner-benchmark</artifactId>
    </dependency>
    <dependency>
      <groupId>org.kie.planner</groupId>
      <artifactId>drools-planner-examples</artifactId>
    </dependency>
    <!-- External dependencies -->
    <!-- Commons -->
    <dependency>
      <groupId>commons-lang</groupId>
      <artifactId>commons-lang</artifactId>
    </dependency>
    <!-- Logging -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
    </dependency>
    <dependency>
      <groupId>ch.qos.logback</groupId>
      <artifactId>logback-classic</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- Xstream -->
    <dependency>
      <groupId>com.thoughtworks.xstream</groupId>
      <artifactId>xstream</artifactId>
    </dependency>

    <dependency>
      <groupId>javax.servlet</groupId>
      <artifactId>servlet-api</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

</project>
