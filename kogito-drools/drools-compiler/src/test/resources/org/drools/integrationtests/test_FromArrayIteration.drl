package org.kie.test;

import  org.kie.DomainObject
import  org.kie.DomainObjectHolder;

global java.util.List list

rule "Test Rule"
when
    $holder : DomainObjectHolder()
    $object : DomainObject( $message : message) from $holder.objects;
then
    list.add( $message );
end 