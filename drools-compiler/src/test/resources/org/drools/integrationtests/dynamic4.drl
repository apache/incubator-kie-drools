package org.drools.test;

import org.drools.Cheese;
import org.drools.Person;

rule "Who likes Stilton"
    when
        Cheese( $type:type == "stilton" )    
        $person:Person( likes == $type )
    then
        System.out.println( $person.getName() + " likes " + $type);
end    