package org.kie;

import org.kie.Query;
import org.kie.Results;
import org.kie.Storage;

rule "Verify_1"
    when
        content : Storage()
        results : Results( ) from content.search(new Query("test","field"))
    then
        System.out.println( results );
end 