<?xml version="1.0" encoding="UTF-8"?>
<section>
  <title>Getting the examples</title>

  <para>Some examples are included as "integration tests" in the Drools source code. On top of these, you can download the drools-examples module. This module is always having more samples added to it by users. A simple way to try it out is to use the Rule Workbench (Eclipse 3.2 + the plugin), and load up the drools-examples project (it has an eclipse project already setup). The rules all have example classes that execute the rules.</para>

  <para>The examples can be found at http://anonsvn.labs.jboss.com/labs/jbossrules/trunk/drools-examples/ (you can browse online, or use a subversion client and download the project into Eclipse</para>


</section>