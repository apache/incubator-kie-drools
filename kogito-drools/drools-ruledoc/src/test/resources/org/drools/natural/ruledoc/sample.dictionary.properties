#The rules stay in the policy document, the code stays here.
Today=(new java.util.Date())
before=${left}.before(${right}) 
date\ of=convertToDate("${right}")
Log=System.out.println(${right})
less_than= <= 
equals=${left}.equals(${right})
or=||
age_of=${right}.getAge()
#you can use spaces, just escape them as per properties file standards (see "date of" above).

#specify imports, comma seperated
imports=java.util.Date, com.yourcompany.Type

#and some functions if you need them,
#remember to use the backslash to span lines, as per properties file stadards
functions=\
public Date convertToDate(Date something) { \
	return null;\
}

#comma seperate app data declarations
application-data=id Type, anotherId AnotherType 

#an overall ruleset name
ruleset-name=My ruleset
