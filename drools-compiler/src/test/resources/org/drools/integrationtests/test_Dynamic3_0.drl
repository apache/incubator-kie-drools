package org.kie.newtest;

import org.kie.Person; 

global java.util.List results

rule "match Person"
        salience 30
    when
        $person : Person()
    then
        results.add( $person );
end    