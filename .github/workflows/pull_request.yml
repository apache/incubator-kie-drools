name: Build Chain

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths-ignore:
      - 'LICENSE*'
      - '.gitignore'
      - '*.md'
      - '*.txt'
jobs:
  build-chain:
    concurrency:
      group: pull_request-${{ github.head_ref }}
      cancel-in-progress: true
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        java-version: [8, 11]
        maven-version: ['3.8.1']
      fail-fast: false
    runs-on: ${{ matrix.os }}
    name: Maven Build
    steps:
      - name: Support longpaths
        if: ${{ matrix.os == 'windows-latest' }}
        uses: ginxo/droolsjbpm-build-bootstrap/.ci/actions/long-paths@BXMSPROD-1539
      - name: Java and Maven Setup
        uses: ginxo/droolsjbpm-build-bootstrap/.ci/actions/java@BXMSPROD-1539
        with:
          java-version: ${{ matrix.java-version }}
          maven-version: ${{ matrix.maven-version }}
      - name: Cache Maven
        uses: ginxo/droolsjbpm-build-bootstrap/.ci/actions/cache-maven@BXMSPROD-1539
        with:
          key-prefix: ${{ runner.os }}-${{ matrix.java-version }}-maven${{ matrix.maven-version }}
      - name: Build Chain
        uses: ginxo/droolsjbpm-build-bootstrap/.ci/actions/build-chain@BXMSPROD-1539
        with:
          annotations-prefix: ${{ runner.os }}-${{ matrix.java-version }}/${{ matrix.maven-version }}
          github-token: "${{ secrets.GITHUB_TOKEN }}"
      - name: Surefire Report
        uses: ginxo/droolsjbpm-build-bootstrap/.ci/actions/surefire-report@BXMSPROD-1539
        if: ${{ always() }}
